server:
  port: 8080

springdoc:
  swagger-ui:
    path: /swagger-ui.html

spring:
  application:
    name: transaction-bff
  redis:
    port: 6379
    host: localhost
    timeout: 5000

limites:
  url: http://localhost:8081/limite-diario
